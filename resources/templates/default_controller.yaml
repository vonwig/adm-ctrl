resources:
  - ../../controller
secretGenerator:
- envs:
  - endpoint.env
  name: endpoint
patchesJson6902:
- target:
    group: apps
    version: v1
    kind: Deployment
    name: policy-controller
  patch: |-
    - op: replace
      path: /spec/template/spec/containers/0/env/2/value
      value: "sandbox"

